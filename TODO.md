# Card Pulse 開発 TODO

最終更新: 2026-09-04

## 1. 目標

Card Pulse は、複数の情報源から TCG カードの価格観測値を継続的に保存し、Card Digger が仕入れ候補を評価できる相場情報を提供する独立したデータ基盤とする。

最初に証明することは「完全自動収集できるか」ではなく、次の 3 点である。

1. 異なる情報源の価格を、出典を失わず同じ形式で蓄積できる。
2. 同じカードを十分な精度で同定し、店舗差と時系列変化を比較できる。
3. 蓄積した情報が、仕入れ判断または売却先判断に実際に役立つ。

## 2. 現状と判断

- リポジトリには構想・調査文書だけがあり、アプリケーションコード、データ、テスト、実行環境はまだない。
- Card Digger と相場データ基盤は責務を分離する。Card Digger は商品発見と評価を担当し、この基盤は価格の取得・保存・正規化・集計を担当する。
- 取得元の優先順位は `公開 API / JSON > HTML > PDF > Web 画像 > X 公式 API > X 手動取込 > X 非公式取得` とする。
- X は MVP の必須依存にしない。twscrape は個人・非商用・小規模の実験には採用可能だが、Web と手動取込で X データの追加価値を測った後に判断する。
- MVP の永続化には SQLite を第一候補とする。ただし、保存形式と利用側の境界を分け、将来 PostgreSQL へ移行可能にする。
- 取得した原本と価格観測値は追記型で保存する。再取得や再解析が可能なように、URL、取得日時、公開日時、ハッシュ、解析バージョンを残す。
- OCR やカード同定の低信頼データは確定値に混ぜず、人間が確認できるレビュー待ち状態にする。

## 3. MVP の範囲

対象は情報源調査の結果から選ぶ。最初から特定の TCG や店舗を固定せず、構造化データの入手しやすさを優先する。

| 項目 | MVP の範囲 |
| --- | --- |
| TCG | 1 種類 |
| 店舗 | 構造化された Web 情報源 2〜3 店舗 |
| 補助入力 | CSV または JSON による手動取込 1 系統 |
| 価格 | 買取価格を優先 |
| 保存 | 原本メタデータ、店舗、カード候補、価格観測値、取得・公開日時 |
| 出力 | カード別の最新価格、中央値、最高値、店舗数、鮮度、価格履歴 |
| 実行 | ローカルで再現可能な CLI と定期実行想定 |

MVP では X の全自動監視、画像認識によるカード画像同定、全 TCG・全店舗対応、リアルタイム更新、クラウド運用、Card Digger UI を行わない。

## 4. 完了条件

MVP は次を満たしたとき完了とする。

- 2〜3 店舗から同じ手順で繰り返しデータを取り込める。
- 同一原本を再投入しても価格観測値が重複しない。
- すべての価格観測値から原本 URL または手動投入ファイルまで追跡できる。
- 金額、通貨、取得日時、店舗、TCG が欠けたデータは確定データに入らない。
- カード同定が曖昧なデータをレビュー待ちとして隔離できる。
- 任意のカードについて、最新価格、中央値、最高値、情報源数、最終更新日時、履歴を表示または JSON 出力できる。
- Collector の一つが停止しても、保存済みデータの照会と他の Collector は動作する。
- 単体テストと固定サンプルを使った Collector の回帰テストが通る。
- 2〜4 週間の試験収集後、データの有用性と次フェーズへ進む判断を記録する。

## 5. 実行計画

### Phase 0 — 対象と情報源を決める

- [ ] 候補となる TCG と店舗を列挙し、`docs/source-map.md` を作る。
- [ ] 各情報源について、URL、取扱 TCG、API/JSON/HTML/PDF/画像の別、更新頻度、カード番号の有無、状態条件、自動取得難易度を記録する。
- [ ] robots.txt、利用規約、アクセス制限、再利用条件、適切な取得間隔を確認し、確認日と根拠 URL を記録する。
- [ ] 同じカードが 2 店舗以上に存在し、カード番号または同等の識別子が得られる候補を優先する。
- [ ] 代表サンプルを各候補から少量だけ手動保存し、解析可能性を比較する。
- [ ] `1 TCG × 2〜3 店舗` を選び、選定理由と見送った候補を ADR に残す。

**完了条件:** 対象、情報源、取得間隔、利用上の制約が決まり、後続タスクが特定サイトの調査待ちになっていない。

### Phase 1 — 最小の土台を作る

- [ ] 使用言語、パッケージ管理、DB、マイグレーション手段を ADR で決める。初期案は Python + SQLite。
- [ ] ローカルセットアップ、テスト、lint、型チェックを一つの手順で実行できるようにする。
- [ ] 秘密情報、取得原本、ローカル DB を誤ってコミットしない `.gitignore` を用意する。
- [ ] 設定をコードから分離し、情報源ごとの有効化、取得間隔、保存先を変更可能にする。
- [ ] ログに `run_id`、source、開始・終了時刻、取得件数、保存件数、エラー分類を出す。
- [ ] 失敗時に途中データを確定せず、同じ入力から安全に再実行できる仕組みを用意する。

**完了条件:** 新しい環境で README の手順どおりにセットアップし、空 DB の作成とテスト実行ができる。

### Phase 2 — データ契約と永続化を作る

- [ ] 最小エンティティを設計する: `source`、`shop`、`ingest_run`、`raw_artifact`、`card_identity`、`price_observation`、`review_item`。
- [ ] `price_observation` に価格、通貨、価格種別、状態条件、公開日時、取得日時、有効期限、source ID/URL を保持する。
- [ ] 原本の content hash と情報源内 ID による重複防止規則を決める。
- [ ] 保存済み観測値を上書きせず、訂正や再解析を履歴として表現する規則を決める。
- [ ] Collector が共通形式を返す境界を定義し、取得方法固有の情報をコア層へ漏らさない。
- [ ] 生データから正規化データへの変換に parser/version を記録する。
- [ ] 正常系、重複、欠損、不正金額、再解析、ロールバックのテストを作る。

**完了条件:** 固定した JSON/CSV サンプルを投入し、追跡可能で重複のない観測値を再現できる。

### Phase 3 — 最初の Web Collector を縦に通す

- [ ] 最も構造化された情報源を 1 つ選び、取得、原本保存、解析、正規化、DB 保存まで実装する。
- [ ] User-Agent、タイムアウト、低頻度アクセス、指数バックオフ、最大再試行回数を設定する。
- [ ] サイトへ再アクセスしなくても解析テストができるよう、許容される固定 fixture を用意する。
- [ ] DOM/JSON 変更を検知できるよう、「0 件」「必須項目欠損」「件数急減」をエラーまたは警告にする。
- [ ] 同じ Collector を連続実行し、冪等性を確認する。
- [ ] 2 つ目、3 つ目の情報源を追加し、共通契約が特定店舗に偏っていないか見直す。

**完了条件:** 2〜3 店舗のデータが同じ観測値モデルに入り、一つの Collector の失敗が他へ波及しない。

### Phase 4 — カード同定と相場照会を作る

- [ ] カードの正規化キー候補を検証する: TCG、セット、カード番号、レアリティ、版、言語。
- [ ] 原文表記と正規化表記を両方保存し、別名を後から追加できるようにする。
- [ ] 完全一致、自動候補、レビュー必須、未同定の状態を区別する。
- [ ] 誤結合を避けるため、カード名だけでは自動確定しない規則をテストする。
- [ ] 最新価格、中央値、最高値、最低値、店舗数、鮮度、スプレッドを計算する query/service を実装する。
- [ ] 外れ値を削除せず表示し、集計値と個別観測値の両方を確認可能にする。
- [ ] CLI またはローカル JSON 出力で、カード別サマリーと履歴を確認できるようにする。

**完了条件:** 複数店舗に存在する代表カードについて、根拠となる観測値付きで価格比較と履歴を再現できる。

### Phase 5 — 試験運用して価値を判定する

- [ ] ローカルスケジューラで低頻度の増分取得を行う。
- [ ] 2〜4 週間、成功率、変更検知、重複率、未同定率、レビュー時間を記録する。
- [ ] 代表カード群について、店舗差と価格変化が仕入れ・売却判断に有用だった事例を記録する。
- [ ] データ欠損時に古い価格を新しい価格として扱わないよう、鮮度表示を確認する。
- [ ] バックアップと空環境への復元を一度実施する。
- [ ] 継続、対象変更、中止の判断と理由を ADR に残す。

**完了条件:** 技術的に取得できたことではなく、利用価値、維持時間、データ品質を数値と事例で説明できる。

### Phase 6 — 手動画像取込と OCR を検証する

Phase 5 で構造化 Web データだけでは不足すると判明した場合に進む。

- [ ] 画像、店舗、投稿 URL、公開日時を一緒に受け取る手動取込口を作る。
- [ ] 原画像を先に保存し、OCR 結果は派生データとして保存する。
- [ ] 代表レイアウトを集め、カード名、番号、価格、状態条件、有効期限の抽出精度を測る。
- [ ] 項目単位の confidence とレビュー待ちキューを実装する。
- [ ] OCR の誤りを訂正しても原文と過去結果が残るようにする。
- [ ] 1 枚当たりのレビュー時間と誤認識率を記録し、継続可否を判断する。

**完了条件:** 精度だけでなく人間の修正時間を含め、手入力より有利か判断できる。

### Phase 7 — X 自動取得の判断と限定実験

Phase 5〜6 のデータで、X 限定情報の利益が取得・保守コストを上回る場合だけ進む。

- [ ] Web と X の重複率、X 限定情報数、判断への寄与、想定月間取得件数を測る。
- [ ] X 公式 API の最新の料金、Rate Limit、利用規約をその時点で再確認する。
- [ ] 公式 API と twscrape の費用、規約、停止リスク、月間保守時間を比較し ADR を作る。
- [ ] twscrape を使う場合は、専用アカウント、`TWS_TELEMETRY=0`、厳密なバージョン固定、独立ミラーを必須にする。
- [ ] X 固有コードを交換可能な Collector 内に閉じ込め、利用側は保存済みデータだけを見る。
- [ ] 取得は特定 List/アカウントの低頻度な増分に限定し、再取得しない。
- [ ] 障害修正時間を記録し、1 回の修正が許容時間を継続的に超えた場合の撤退条件を決める。

**完了条件:** X が翌日停止しても既存機能と保存済みデータが利用でき、採用理由と撤退条件が明文化されている。

### Phase 8 — Card Digger と連携する

相場データの有用性が確認できた後に進む。

- [ ] Card Digger が必要とする問い合わせをユースケースから定義する。
- [ ] 入力するカード識別子、曖昧一致の扱い、情報鮮度、欠損時の応答を決める。
- [ ] まず同一プロセスまたはファイル出力で契約を検証し、必要なら HTTP API 化する。
- [ ] API を作る場合は、バージョニング、ページング、エラー形式、観測値への参照を定義する。
- [ ] Card Digger 側の出品価格と、保守的な相場値を比較する評価ロジックを別責務として実装する。
- [ ] 最高買取価格だけで掘り出し物判定を行わない。中央値、店舗数、鮮度、ばらつきを返す。

**完了条件:** Card Digger が相場基盤の内部 DB を直接参照せず、根拠と鮮度を含む価格情報を取得できる。

## 6. 最初に着手する順序

次の作業セッションでは、以下をこの順で行う。

1. 情報源マップのテンプレートを作る。
2. 候補店舗を調査して 1 TCG × 2〜3 店舗を選ぶ。
3. 代表レスポンスまたはファイルで、カード番号・価格・条件・日時が取得できるか確認する。
4. 技術選定 ADR と最小データ契約を書く。
5. 最初の Collector を、取得から照会まで縦に 1 本通す。

大きな共通基盤を先に作らず、最初の実データを通した結果から二つ目の情報源に必要な抽象化だけを追加する。

## 7. 保留する判断

次の項目は検証結果なしに確定しない。

- 正式な対象 TCG と店舗数
- PostgreSQL やクラウドへの移行
- X 公式 API または twscrape の本採用
- OCR/Vision 製品の選定
- 自動カード画像認識
- 相場スコアと掘り出し物スコアの正式な式
- Card Digger との通信方式
- 商用利用や第三者提供

## 8. 常設の品質・安全チェック

- [ ] 取得元の規約と負荷を尊重し、制約が不明な情報源は自動取得しない。
- [ ] 認証情報、Cookie、個人情報をログ、fixture、Git に残さない。
- [ ] 金額は整数の最小通貨単位で保存し、表示文字列を直接計算に使わない。
- [ ] タイムゾーンを明示し、公開日時と取得日時を混同しない。
- [ ] 「データなし」「古いデータ」「価格 0 円」を区別する。
- [ ] parser の変更前後で固定サンプルの結果を比較する。
- [ ] 取得失敗を正常な 0 件として扱わない。
- [ ] 自動同定の誤結合を、未同定より重大な障害として扱う。
- [ ] 集計値から原本まで追跡できる状態を維持する。
- [ ] 保存データのバックアップと復元手順を定期的に確認する。

